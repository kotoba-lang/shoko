(ns shoko.archiveport
  "Status: proposed — 雛形のみ。governor/operation の実装は follow-up
  （ADR-2607062020）。将来の ArchivePort protocol 候補: `fetch-file`
  `propose-revision!` `share!`（承認後のみ）。")

(defn placeholder []
  :not-implemented)
